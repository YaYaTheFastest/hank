# Castle Fund V2 — shipped vs remaining

The 2026-07-28 TanStack Start / PGLite standalone dump and its paste-prompt are retired.
Do **not** re-implement check-in, XP, bundle progress, or the parent sheet — those already live in `castle.js` and the Worker.

**Truth:** `hank-context.md` (LIVE STATE + CASTLE FUND V2 PLAN) and the live Castle pages.

## Already shipped (do not rebuild)

- Work-day check-in (+10 XP, once per chore-day)
- XP + levels on approve
- Bundle `progress` + `competitionVisible` defaults (old KV still loads)
- Parent PIN approve/decline, catalog/config, deduct, interest, purchase (kid-page parent sheet)

## Remaining gaps (not new scope)

Track in hank-context / live Castle (Area F / #9). Do not invent extra product.

- Money lessons UI (unlock by level)
- Competition work arena
- Wishlist product search

## Out of scope

- TanStack / React / PGLite port
- Worker, Castle client, or `index.html` edits in this Area H change
